apiVersion: v1
kind: Service
metadata:
  name: {{ include "ctms-api.fullname" . }}
  labels:
    {{- include "ctms-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ctms-api.selectorLabels" . | nindent 4 }}
